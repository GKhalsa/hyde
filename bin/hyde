#!/usr/bin/env ruby
require_relative '../lib/structure'

query = ARGV[0]
file_path = ARGV[1]
structure = Structure.new
output = Output.new

  if query == "new"
    structure.create_tree(file_path)
  elsif query == "build"
    output.build_html
  else
    "Invalid Commmand"
  end
